import { TooltipbtmDirective } from './tooltipbtm.directive';

describe('TooltipbtmDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipbtmDirective();
    expect(directive).toBeTruthy();
  });
});
